import type { Server } from 'node:http';
import type { Express } from 'express';

/**
 * Placeholder for Socket.io / native WebSocket setup.
 * Wire real-time features here when needed.
 */
export function registerWebSocket(_app: Express, _httpServer: Server): void {
  // no-op
}
